/* Simple HAL/S test */

PROGRAM SIMPLE;

  DECLARE X SCALAR;
  DECLARE Y SCALAR;
  DECLARE V VECTOR(3);

  X = 1.0;
  Y = 2.0;
  V = VECTOR(X, Y, 3.0);

  IF X > 0.0 THEN
    Y = X + 1.0;
  END;

  DO FOR X = 1 TO 10;
    Y = Y + X;
  END;

CLOSE SIMPLE;
